## Info
Default user: dev
Default password: devpass

## Post installation
Install packer dependencies
```bash
cd ~/.config/nvim
nvim
```
vim editor commands
```vim
:e lua/jy/packer.lua
:so
:PackerSync
```
